<h1 class="mt-12 text-4xl text-center">講師課程</h1>
<%= render "shared/flash" if flash %>

<div class="flex justify-between">
  <div>
    <% @lecturers.each do |lecturer|%>
    <ul>
      <li><%= link_to lecturer.name, lecturer_path(lecturer.id) %></li>
    </ul>
    <% end %>
  </div>
  
    <div>課程搜尋</div>
</div>

<div class="flex flex-wrap mx-auto md:container">
	<%= render partial: "courses/course", collection: @lecturer.courses.where(published: "publish") %>
</div>